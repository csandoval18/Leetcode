const addBinary = (a, b) => {
	let re = 1
	let ans = "",
		aLength = a.length,
    bLength = b.length
	while (re) {
    if ()
		if (a[a.length - 1] === "1" && b[b.length] === "1") {
			ans = "1" + ans
		} else {
		}
	}
}
